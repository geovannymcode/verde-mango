# URL base del backend (solo se usa en build de producción; en dev se usa el proxy de Vite)
VITE_API_BASE_URL=http://localhost:8080

# Llave pública de Wompi (sandbox). No incluir llaves privadas aquí.
VITE_WOMPI_PUBLIC_KEY=
